# 🏗️ Authentication Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                    http://localhost:5173/                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (React + Vite)                       │
│                    Port: 5173                                    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  Public Routes (No Auth Required)                       │   │
│  │  • /login          → LoginPage                          │   │
│  │  • /signup         → SignupPage                         │   │
│  │  • /verify-otp     → VerifyOTPPage                      │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  Protected Routes (Auth Required)                       │   │
│  │  • /your-courses   → App (Your Courses)                 │   │
│  │  • /admin          → AdminPage                          │   │
│  │                                                          │   │
│  │  ProtectedRoute Component:                              │   │
│  │  - Checks if user is authenticated                      │   │
│  │  - Redirects to /login if not                           │   │
│  │  - Preserves intended destination                       │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  AuthContext (State Management)                         │   │
│  │  • user: User object or null                            │   │
│  │  • loading: Boolean                                     │   │
│  │  • login(token, user): Function                         │   │
│  │  • logout(): Function                                   │   │
│  │  • Validates token on app load                          │   │
│  │  • Stores token in localStorage                         │   │
│  └────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP Requests
                              │ /api/* → Proxied to backend
                              │ Authorization: Bearer <token>
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   BACKEND (Express + Node.js)                    │
│                    Port: 3000                                    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  Public API Endpoints (No Auth)                         │   │
│  │  POST /api/auth/signup       → Register user            │   │
│  │  POST /api/auth/login        → Login user               │   │
│  │  POST /api/auth/verify-otp   → Verify email             │   │
│  │  POST /api/auth/resend-otp   → Resend OTP               │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  Protected API Endpoints (JWT Required)                 │   │
│  │  GET  /api/auth/me           → Get current user         │   │
│  │  POST /api/auth/logout       → Logout user              │   │
│  │  GET  /api/courses           → Get all courses          │   │
│  │                                                          │   │
│  │  Auth Middleware (verifyToken):                         │   │
│  │  - Extracts JWT from Authorization header               │   │
│  │  - Verifies token signature                             │   │
│  │  - Checks expiration                                    │   │
│  │  - Returns 401 if invalid                               │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  Services                                                │   │
│  │  • Email Service (OTP sending)                          │   │
│  │  • JWT Token Generation                                 │   │
│  │  • Password Hashing (bcrypt)                            │   │
│  └────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Database Queries
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      MongoDB Atlas (Cloud)                       │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  Collections:                                            │   │
│  │  • users                                                 │   │
│  │    - name, email, username                              │   │
│  │    - password (hashed)                                  │   │
│  │    - isEmailVerified                                    │   │
│  │    - otp, otpExpiration                                 │   │
│  │                                                          │   │
│  │  • courses                                               │   │
│  │    - title, description, modules                        │   │
│  │    - progress, status                                   │   │
│  └────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Authentication Flow Diagrams

### 1. Signup Flow

```
┌──────────┐
│  User    │
└────┬─────┘
     │
     │ 1. Visits /signup
     ↓
┌────────────────┐
│  SignupPage    │
└────┬───────────┘
     │
     │ 2. Fills form & submits
     ↓
┌────────────────┐
│  POST /api/    │
│  auth/signup   │
└────┬───────────┘
     │
     │ 3. Validates data
     ↓
┌────────────────┐
│  Check if user │
│  exists in DB  │
└────┬───────────┘
     │
     │ 4. Generate OTP
     ↓
┌────────────────┐
│  Save user to  │
│  MongoDB       │
└────┬───────────┘
     │
     │ 5. Send OTP email
     ↓
┌────────────────┐
│  Email Service │
└────┬───────────┘
     │
     │ 6. Redirect to /verify-otp
     ↓
┌────────────────┐
│  VerifyOTPPage │
└────┬───────────┘
     │
     │ 7. User enters OTP
     ↓
┌────────────────┐
│  POST /api/    │
│  auth/verify   │
│  -otp          │
└────┬───────────┘
     │
     │ 8. Verify OTP
     ↓
┌────────────────┐
│  Update user   │
│  isEmailVerified│
│  = true        │
└────┬───────────┘
     │
     │ 9. Generate JWT
     ↓
┌────────────────┐
│  Return token  │
│  & user data   │
└────┬───────────┘
     │
     │ 10. Store token in localStorage
     ↓
┌────────────────┐
│  Redirect to   │
│  /your-courses │
└────────────────┘
```

### 2. Login Flow

```
┌──────────┐
│  User    │
└────┬─────┘
     │
     │ 1. Visits /login
     ↓
┌────────────────┐
│  LoginPage     │
└────┬───────────┘
     │
     │ 2. Enters credentials
     ↓
┌────────────────┐
│  POST /api/    │
│  auth/login    │
└────┬───────────┘
     │
     │ 3. Find user in DB
     ↓
┌────────────────┐
│  Check if      │
│  email verified│
└────┬───────────┘
     │
     │ 4. Verify password (bcrypt)
     ↓
┌────────────────┐
│  Generate JWT  │
│  token         │
└────┬───────────┘
     │
     │ 5. Return token & user
     ↓
┌────────────────┐
│  Store token   │
│  in localStorage│
└────┬───────────┘
     │
     │ 6. Update AuthContext
     ↓
┌────────────────┐
│  Redirect to   │
│  /your-courses │
└────────────────┘
```

### 3. Protected Route Access

```
┌──────────┐
│  User    │
└────┬─────┘
     │
     │ 1. Tries to access /your-courses
     ↓
┌────────────────┐
│  ProtectedRoute│
│  Component     │
└────┬───────────┘
     │
     │ 2. Check AuthContext
     ↓
┌────────────────┐
│  Is user       │
│  authenticated?│
└────┬───────────┘
     │
     ├─── NO ──→ ┌────────────────┐
     │           │  Save intended │
     │           │  destination   │
     │           └────┬───────────┘
     │                │
     │                │ Redirect to /login
     │                ↓
     │           ┌────────────────┐
     │           │  LoginPage     │
     │           └────┬───────────┘
     │                │
     │                │ After login
     │                ↓
     │           ┌────────────────┐
     │           │  Redirect to   │
     │           │  saved dest    │
     │           └────────────────┘
     │
     └─── YES ──→ ┌────────────────┐
                  │  Render        │
                  │  /your-courses │
                  └────────────────┘
```

### 4. API Request with JWT

```
┌──────────┐
│  Frontend│
└────┬─────┘
     │
     │ 1. User action (e.g., load courses)
     ↓
┌────────────────┐
│  Get token from│
│  localStorage  │
└────┬───────────┘
     │
     │ 2. Add to Authorization header
     ↓
┌────────────────┐
│  GET /api/     │
│  courses       │
│  Authorization:│
│  Bearer <token>│
└────┬───────────┘
     │
     │ 3. Request reaches backend
     ↓
┌────────────────┐
│  Auth Middleware│
│  (verifyToken) │
└────┬───────────┘
     │
     │ 4. Extract token from header
     ↓
┌────────────────┐
│  Verify token  │
│  with JWT_SECRET│
└────┬───────────┘
     │
     ├─── INVALID ──→ ┌────────────────┐
     │                │  Return 401    │
     │                │  Unauthorized  │
     │                └────┬───────────┘
     │                     │
     │                     │ Frontend receives 401
     │                     ↓
     │                ┌────────────────┐
     │                │  Clear token   │
     │                │  Redirect to   │
     │                │  /login        │
     │                └────────────────┘
     │
     └─── VALID ──→ ┌────────────────┐
                    │  Attach userId │
                    │  to request    │
                    └────┬───────────┘
                         │
                         │ 5. Process request
                         ↓
                    ┌────────────────┐
                    │  Query MongoDB │
                    └────┬───────────┘
                         │
                         │ 6. Return data
                         ↓
                    ┌────────────────┐
                    │  Send response │
                    │  to frontend   │
                    └────────────────┘
```

### 5. Logout Flow

```
┌──────────┐
│  User    │
└────┬─────┘
     │
     │ 1. Clicks logout button
     ↓
┌────────────────┐
│  Header        │
│  Component     │
└────┬───────────┘
     │
     │ 2. Call logout()
     ↓
┌────────────────┐
│  AuthContext   │
│  logout()      │
└────┬───────────┘
     │
     │ 3. Remove token from localStorage
     ↓
┌────────────────┐
│  Clear user    │
│  state         │
└────┬───────────┘
     │
     │ 4. Show success toast
     ↓
┌────────────────┐
│  Navigate to   │
│  /login        │
└────────────────┘
```

---

## Component Hierarchy

```
App (Root)
│
├── AuthProvider (Context)
│   │
│   ├── BrowserRouter
│   │   │
│   │   └── Routes
│   │       │
│   │       ├── / → Navigate to /login
│   │       │
│   │       ├── /login → LoginPage
│   │       │
│   │       ├── /signup → SignupPage
│   │       │
│   │       ├── /verify-otp → VerifyOTPPage
│   │       │
│   │       ├── /your-courses → ProtectedRoute
│   │       │   └── App (Main Course Page)
│   │       │       ├── Sidebar
│   │       │       ├── Header
│   │       │       ├── PageHeader
│   │       │       ├── FilterChips
│   │       │       ├── CourseCard (multiple)
│   │       │       └── RightSidebar
│   │       │
│   │       └── /admin → ProtectedRoute
│   │           └── AdminPage
│   │
│   └── ErrorBoundary
```

---

## Data Flow

### User State Management

```
┌─────────────────────────────────────────────────────────┐
│                    AuthContext                           │
│                                                          │
│  State:                                                  │
│  • user: { id, name, email, username, isEmailVerified } │
│  • loading: boolean                                     │
│                                                          │
│  Functions:                                              │
│  • login(token, user)                                   │
│  • logout()                                             │
│                                                          │
│  Effects:                                                │
│  • On mount: Check localStorage for token               │
│  • If token exists: Validate with /api/auth/me          │
│  • If valid: Set user state                             │
│  • If invalid: Clear token                              │
└─────────────────────────────────────────────────────────┘
                          │
                          │ Provides to all children
                          ↓
┌─────────────────────────────────────────────────────────┐
│                    All Components                        │
│                                                          │
│  Can access via useAuth():                              │
│  • const { user, loading, login, logout } = useAuth()   │
│                                                          │
│  Examples:                                               │
│  • Header: Display user.name                            │
│  • Sidebar: Display user.name                           │
│  • ProtectedRoute: Check if user exists                 │
│  • LoginPage: Call login() on success                   │
└─────────────────────────────────────────────────────────┘
```

### Token Storage

```
┌──────────────┐
│  localStorage│
│              │
│  Key: token  │
│  Value: JWT  │
└──────┬───────┘
       │
       │ Read on app load
       ↓
┌──────────────┐
│  AuthContext │
│  useEffect   │
└──────┬───────┘
       │
       │ Validate token
       ↓
┌──────────────┐
│  GET /api/   │
│  auth/me     │
└──────┬───────┘
       │
       ├─── Valid ──→ Set user state
       │
       └─── Invalid ──→ Clear localStorage
```

---

## Security Architecture

### Password Security

```
User Password (Plain Text)
        ↓
    bcrypt.hash()
        ↓
Hashed Password (Stored in DB)
        ↓
    Never sent to frontend
        ↓
    Only compared with bcrypt.compare()
```

### JWT Token Structure

```
Header:
{
  "alg": "HS256",
  "typ": "JWT"
}

Payload:
{
  "id": "user_id_from_mongodb",
  "iat": 1234567890,  // Issued at
  "exp": 1234567890   // Expires at (7 days)
}

Signature:
HMACSHA256(
  base64UrlEncode(header) + "." +
  base64UrlEncode(payload),
  JWT_SECRET
)
```

### Request Flow with Security

```
Frontend Request
    ↓
Add JWT to Authorization header
    ↓
Send to Backend
    ↓
CORS Check (Origin validation)
    ↓
Auth Middleware (JWT verification)
    ↓
Route Handler (Business logic)
    ↓
Database Query
    ↓
Response (JSON only, no sensitive data)
    ↓
Frontend receives data
```

---

## Error Handling Flow

```
┌──────────────┐
│  API Request │
└──────┬───────┘
       │
       ↓
┌──────────────┐
│  Try/Catch   │
└──────┬───────┘
       │
       ├─── Success ──→ Return data
       │
       └─── Error ──→ ┌──────────────┐
                      │  Check error │
                      │  type        │
                      └──────┬───────┘
                             │
                             ├─── 401 ──→ Logout & redirect to /login
                             │
                             ├─── 403 ──→ Show "Access denied"
                             │
                             ├─── 404 ──→ Show "Not found"
                             │
                             ├─── 500 ──→ Show "Server error"
                             │
                             └─── Network ──→ Show "Connection error"
```

---

## Deployment Architecture

### Development
```
Frontend: http://localhost:5173/
Backend:  http://localhost:3000/
Database: MongoDB Atlas (Cloud)
```

### Production (Example)
```
Frontend: https://jgeclearn.com/
Backend:  https://api.jgeclearn.com/
Database: MongoDB Atlas (Cloud)
```

---

## Summary

This architecture provides:

✅ **Separation of Concerns**
   - Frontend: UI only
   - Backend: API only
   - Database: Data storage

✅ **Security**
   - JWT authentication
   - Password hashing
   - Token validation
   - Protected routes

✅ **Scalability**
   - Stateless backend
   - Token-based auth
   - Cloud database

✅ **User Experience**
   - Seamless authentication
   - Persistent sessions
   - Automatic redirects
   - Clear error messages

✅ **Maintainability**
   - Clean code structure
   - Reusable components
   - Centralized state management
   - Comprehensive error handling
